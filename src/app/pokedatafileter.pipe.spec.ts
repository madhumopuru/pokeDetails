import { PokedatafileterPipe } from './pokedatafileter.pipe';

describe('PokedatafileterPipe', () => {
  it('create an instance', () => {
    const pipe = new PokedatafileterPipe();
    expect(pipe).toBeTruthy();
  });
});
